import { Namespace } from "../Namespace";
export declare class Species extends Namespace {
    constructor(data: any);
    sentient(ctx?: string | string[]): any;
    nonsentient(ctx?: string | string[]): any;
    any(ctx?: string | string[]): any;
}
